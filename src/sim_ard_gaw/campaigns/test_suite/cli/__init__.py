"""CLI entry points.

Each module here is runnable as `python -m test_suite.cli.<name>` from
the `scripts/` directory. The flag surface mirrors the legacy script of
the same role so existing operator runbooks keep working when the entry
point is swapped.
"""
