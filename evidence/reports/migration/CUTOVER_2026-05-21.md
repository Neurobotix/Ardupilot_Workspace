# Phase 7 Cutover And Old Workspace Deprecation

Date/time: 2026-05-21T21:44:11+03:00

Timezone: Africa/Cairo / EEST (+03:00)

Decision: BLOCKED

Old workspace deprecation status: NOT DEPRECATED

## 2026-05-22 Policy Update

This report preserves the 2026-05-21 blocked cutover attempt. Its cleanup and
installed-plugin blocker analysis was superseded on 2026-05-22 by
`governance/decisions/ADR-0004-clean-run-and-workspace-plugin-policy.md` and
the matching runtime/doc updates:

- broad cleanup is accepted as intentional clean-run safety policy before a
  governed simulation run;
- setup, launch, and wind-matrix runtime now use only
  `build/ardupilot_gazebo/libArduPilotPlugin.so`;
- installed Gazebo plugin fallback is forbidden and missing workspace build
  output fails closed.

Phase 7 remains `BLOCKED` until final shadow parity and representative workflow
proof are rerun under that current policy. This addendum does not promote
`workspace_next`, deprecate the old workspace, or create a cutover ADR.

Current post-`ADR-0004` blocker set:

1. rerun final Phase 7 shadow parity under the clean-run and workspace-plugin
   policy;
2. record fresh representative vehicle workflow proof under that policy;
3. record fresh representative campaign/evidence workflow proof under that
   policy.

The sections marked historical below preserve why the original 2026-05-21
cutover attempt stopped. They are not the live blocker set after the 2026-05-22
policy update.

Safe remediation checks run for this update:

- `make doctor`;
- `scripts/maintenance/validate_structure.sh`;
- `scripts/maintenance/validate_evidence.sh`;
- `make test-parity` with six parity tests passing;
- Python bytecode compilation for the touched wind-runner and parity-test
  files;
- `pyright tests/parity/test_phase1_parity.py`;
- a sourced `setup.bash` shell check proving
  `GZ_SIM_SYSTEM_PLUGIN_PATH` equals the workspace build directory and the
  workspace plugin file exists;
- `scripts/ops/launch.sh help`.

The broader `pyright` attempt over the legacy `run_one.py` and `run_one_og.py`
files still reports unresolved third-party imports and older type issues outside
this policy change.

## Scope

Phase 7 audited whether `ardupilot_workspace_next` can become the production
workspace and whether the old workspace can be deprecated by policy. This pass
improved the Phase 7 runbook first, ran the safe final-readiness checks,
verified bounded vehicle and campaign/evidence proof already available in the
evidence system, and recorded why cutover cannot proceed.

The old workspace was read only for the production-reference status check. It
was not modified.

## Historical 2026-05-21 Readiness Audit

Result: BLOCKED.

This section records the readiness basis at the time of the 2026-05-21 blocked
attempt. The live post-policy blocker set is the missing fresh Phase 7 proof
listed in the 2026-05-22 update above.

The structural and evidence guardrails pass, prior phase reports exist, and the
Phase 6 evidence workflow is operational. Cutover still fails the prerequisite
decision because two production-safety blockers remain open without an accepted
ADR:

1. `src/sim_ard_gaw/compat_scripts/launch.sh` still implements `cleanup` with
   broad `pkill -9` patterns for Gazebo, ArduPilot, MAVProxy, `sim_vehicle.py`,
   and LiDAR bridge processes. `.ai/issues/open.md` already states this can kill
   unrelated simulator or production-reference work and must be scoped before
   cutover.
2. Evidence-producing wind-matrix runs still can fall back to the stale
   installed Gazebo plugin path when the workspace plugin build is absent.
   Phase 5 incident evidence shows that fallback invalidated the first bounded
   `4,4` comparison as ArduPilot-side wind parity proof. No ADR currently
   accepts that risk for production promotion.

Because those blockers threaten production process safety and campaign evidence
integrity, Phase 7 does not create an accepted cutover ADR, does not mark the
old workspace deprecated, and does not promote production-status docs.

## Prior Phase Evidence Summary

| Phase | Latest report | Conclusion | Phase 7 use |
| --- | --- | --- | --- |
| Phase 0 | `evidence/reports/PHASE_0_BASELINE_2026-05-20.md` | Baseline report exists and records the old workspace as production reference. | Reference baseline plus unresolved old-workspace dirty state. |
| Phase 1 | `evidence/reports/PHASE_1_STRUCTURE_2026-05-20.md` | PASS | Structure guardrail basis. |
| Phase 2 | `evidence/reports/PHASE_2_RUNTIME_PARITY_2026-05-20.md` | PASS | Direct launch, bridge, logger, output-home, and representative vehicle proof. |
| Phase 3 | `evidence/reports/PHASE_3_DOCS_2026-05-20.md` | PASS | Canonical docs/status boundary. |
| Phase 4 | `evidence/reports/PHASE_4_CONFIG_ASSETS_2026-05-21.md` | PASS | Asset/config ownership and parameter hash boundary. |
| Phase 5 | `evidence/reports/PHASE_5_CAMPAIGN_TESTS_2026-05-21.md` | PASS | Bounded tiny campaign proof plus retained plugin fallback incident history. |
| Phase 6 | `evidence/reports/PHASE_6_EVIDENCE_OPS_2026-05-21.md` | PASS | Evidence workflow, templates, catalog, and guardrails. |

Phase 0 records a baseline rather than a later `Phase 0 status: PASS` line; the
required dated Phase 0 report exists and establishes the read-only production
reference.

## Historical 2026-05-21 Blocker Review

Cutover blockers at the original blocked attempt:

- unsafe broad cleanup process killing before cutover;
- wind-matrix evidence path does not yet fail closed when the workspace Gazebo
  plugin build is missing and only the installed fallback remains;
- final Phase 7 shadow parity is therefore blocked before fresh
  production-promotion runtime smoke, bridge smoke, and cleanup proof can pass.

Other unresolved migration issues reviewed:

- Phase 0 production dirty state remains recorded;
- `copter-lidar` has handshake/bridge proof but no captured obstacle return;
- non-core launch targets remain not yet runtime verified;
- full wind-matrix evidence beyond bounded Phase 5 proof remains future work;
- legacy compatibility paths remain until Phase 8;
- protobuf duplicate-descriptor warnings remain noisy but non-fatal.

## Commands Run

Readiness and validation commands:

- `sed -n ...` reads of Phase 7/full/shadow runbooks, standards, status docs,
  prior phase reports, Phase 5 ADR/incident, evidence workflow, catalog, and
  blocker-bearing runtime code;
- `rg --files evidence/reports governance/decisions governance/audits evidence/indexes evidence/templates`;
- `rg -n ...` prior-phase conclusion and docs/status consistency scans;
- `bash -lc 'source setup.bash'`;
- `make doctor`;
- `scripts/maintenance/validate_structure.sh`;
- `scripts/maintenance/validate_evidence.sh`;
- `make test-parity`;
- `scripts/ops/launch.sh help`;
- direct static imports of `lidar_bridge_unified`, `log_flight_data`,
  `wind_publisher_altitude`, and `test_suite.cli.run_round_robin`;
- `scripts/ops/launch.sh wind-check-altitude`;
- raw runtime leakage `find` scan with `.git`, `var`, `.private`, dependency,
  env, build, and install homes pruned;
- process probe with `pgrep -af` for Gazebo, ArduPilot, MAVProxy, and LiDAR
  bridge command lines;
- `scripts/ops/capture_round.sh plane`;
- `scripts/ops/capture_round.sh --promote-reviewed plane`;
- read-only `git -C /home/ahmed/ardupilot_workspace status --short`.

The first post-edit `make doctor` and direct structure check failed because the
new Phase 7 runbook briefly used an unallowlisted absolute old-workspace path.
That wording was narrowed before the final passing `make doctor` and structure
validation results below.

## Shadow Parity Result

Final shadow parity result: BLOCKED.

Detailed rows live in
`evidence/reports/shadow_parity_2026-05-21.md`.

Safe Phase 7 shadow-parity items passed:

- environment setup;
- doctor/structure/evidence guardrails;
- launch help surface;
- static import check, with the known protobuf warning noise;
- parity tests;
- output-home and promotion-guard checks.

At the original blocked attempt, fresh cutover runtime smoke, bridge smoke,
wind/CTE promotion proof, and cleanup process hygiene could not produce a
cutover pass until the then-open policy blockers were resolved or explicitly
accepted by governance.

## Representative Vehicle Workflow

Result: VERIFIED from bounded evidence, not promoted as a new live cutover run.

- Command path: `scripts/ops/launch.sh plane` plus
  `scripts/ops/launch.sh gazebo-plane` in the Phase 2 smoke workflow.
- Environment setup: `source setup.bash`.
- Runtime outcome: the curated Phase 2 plane evidence records SITL/Gazebo/MAVLink
  handshake, EKF/GPS readiness, throttle arm, and 17.3 m/s groundspeed.
- Output location: raw MAVProxy telemetry is under `var/logs/mavproxy/plane/`;
  Phase 7 also ran `scripts/ops/capture_round.sh plane`, which wrote a new
  working capture under `var/working/runtime_capture/`.
- Cleanup: Phase 2 records cleanup after the smoke run. At the original
  2026-05-21 attempt, Phase 7 did not reuse the broad cleanup command as
  cutover proof before the clean-run policy decision existed.
- Report/evidence link:
  `evidence/reports/PHASE_2_RUNTIME_PARITY_2026-05-20.md` and
  `evidence/curated_logs/phase_2_runtime_2026-05-20/plane_evidence.txt`.

## Representative Campaign And Evidence Workflow

Result: VERIFIED from bounded Phase 5/6 proof.

- Command path: the bounded Phase 5 `test_suite.cli.run_round_robin` tiny
  `wind_x_00_y_00` workflow recorded in
  `evidence/reports/PHASE_5_CAMPAIGN_TESTS_2026-05-21.md`.
- Parameter/config provenance: the curated tiny manifest records the shared
  `config/vehicles/plane_base.parm` and
  `config/overlays/plane_airspeed.parm` paths, sizes, and SHA-256 values.
- Runtime output location: raw attempt output remains under
  `var/runs/phase5_tiny_rr_20260521_escalated/`.
- Curated evidence/report/index state:
  `evidence/curated_logs/phase5_tiny_rr_20260521/manifest.json`,
  `evidence/reports/PHASE_5_CAMPAIGN_TESTS_2026-05-21.md`, and the
  `phase5-tiny-rr` catalog row.
- Evidence promotion behavior: Phase 7 rechecked that a working plane capture
  stays under `var/` and unversioned reviewed promotion is refused without
  `--evidence-id`.
- Cleanup: the tiny campaign cleanup result remains Phase 5 evidence. Current
  Phase 7 still needs fresh campaign/evidence workflow proof under the
  clean-run policy before cutover can be reconsidered.

## Validation Results

| Check | Result |
| --- | --- |
| `make doctor` | PASS on final run. Structure and evidence validators passed. |
| `scripts/maintenance/validate_structure.sh` | PASS on final run. |
| `scripts/maintenance/validate_evidence.sh` | PASS. |
| `make test-parity` | PASS. Four parity tests ran. |
| Prior report existence/conclusion check | PASS for Phase 0 through Phase 6 latest report presence; Phase 1 through Phase 6 conclude PASS. |
| Raw log leakage scan | PASS. No raw `.BIN`, `.bin`, `.tlog`, or `.tlog.raw` files were returned outside allowed homes after prune rules. |
| Docs/status consistency scan | PASS for no accidental Phase 7 production/deprecation promotion; current docs still surface the not-production and reference-only boundaries. |
| Retired target behavior | PASS for documented behavior. `wind-check-altitude` exited 2 with the retired-target explanation. |
| Static imports | PASS with known protobuf warning noise. |

## Historical 2026-05-21 Cleanup And Process Hygiene

Process hygiene result for the original blocked attempt: BLOCKED.

The pre-cutover probe found no simulator process result that Phase 7 could use
as production-promotion evidence beyond the probe command itself. At that time,
the runtime code inspection found broad `pkill -9` cleanup patterns before the
clean-run policy decision existed. Current proof must rerun cleanup/process
hygiene under `ADR-0004`.

## Output Location And Evidence Policy

Result: PASS for guardrails and promotion behavior.

- `make doctor` and `validate_evidence.sh` passed the raw-output, report-home,
  template, evidence-home, and curated-root catalog checks.
- The explicit raw leakage scan returned no raw telemetry outside allowed homes.
- `scripts/ops/capture_round.sh plane` wrote working output to
  `var/working/runtime_capture/`.
- `scripts/ops/capture_round.sh --promote-reviewed plane` refused reviewed
  promotion without a new evidence ID.

## Docs, AI, Governance, And Evidence Updates

- Tightened `governance/runbooks/phase_7_cutover_deprecation.md` so it encodes
  prerequisite decisions, the blocked path, the report contract, and the exit
  gate.
- Added `governance/runbooks/workspace_cutover_rollback.md`.
- Added this report and the Phase 7 shadow parity report.
- Updated `.ai/current.md`, `.ai/issues/open.md`,
  `docs/operations/migration_status.md`, `.ai/index.md`, and the evidence
  catalog to keep the blocked decision discoverable.
- Did not create an accepted cutover ADR.
- Did not update production-status docs to call `workspace_next` production or
  the old workspace deprecated.

## Cutover Decision

BLOCKED.

Old workspace status stays `NOT DEPRECATED`. The old workspace remains the
production reference and later fallback policy input until a passing cutover
report plus accepted cutover ADR change that status.

Accepted risks: none. No cutover risk was accepted in this blocked pass.

## Unresolved Blockers

Live post-`ADR-0004` blockers:

1. Re-run final Phase 7 shadow parity under the clean-run and workspace-plugin
   policy.
2. Record fresh representative vehicle workflow proof under that policy,
   including cleanup and workspace plugin provenance where runtime behavior
   depends on it.
3. Record fresh representative campaign/evidence workflow proof under that
   policy, including cleanup and workspace plugin provenance.

The existing open-issue list still carries the broader migration follow-ups
reviewed above.

Compatibility retirement remains Phase 8.
