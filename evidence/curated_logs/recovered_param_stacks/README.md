# Recovered Parameter Stacks

Historical parameter-stack evidence belongs here, not in active `config/`.

These files may include local override values captured from production-era
campaigns. Treat them as evidence for comparison and provenance only; do not
load them as active workspace defaults.
