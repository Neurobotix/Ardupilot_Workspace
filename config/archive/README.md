# Archived Configs

This folder keeps older parameter snapshots that were superseded by the current
layered configuration model.

Current contents:

- `plane_all_in_one_legacy.parm`
  Historical mixed airspeed + LiDAR config.
- `plane_benchmark_dualwind_legacy.parm`
  Older benchmark config that also enabled SITL wind.
