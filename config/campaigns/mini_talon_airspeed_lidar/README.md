## mini_talon_airspeed_lidar config lane

This directory owns the runtime parameters for the integrated Mini Talon airspeed + LiDAR stack.

Implemented file:
- `plane_full.parm`

The parameter file is intentionally self-contained:
- airspeed enabled for Gazebo wind worlds
- LiDAR/rangefinder enabled for the project bridge path
- staircase navigation tuning already folded in
