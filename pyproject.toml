[project]
name = "ardupilot-workspace-next"
version = "0.1.0"
description = "Internal ArduPilot Gazebo simulation workspace"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "src/sim_ard_gaw/compat_scripts"]
