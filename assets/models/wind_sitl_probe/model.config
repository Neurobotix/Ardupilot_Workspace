<?xml version="1.0"?>
<model>
  <name>Wind SITL Probe</name>
  <version>1.0</version>
  <sdf version="1.9">model.sdf</sdf>
  <description>
    wind_sensor_probe + ArduPilot plugin + IMU.
    No LiftDrag — wind cannot move it.
    Used for bench_s1_airspeed: proves ArduPilot reads /airspeed from Gazebo wind.
  </description>
</model>
