# Archived Missions

This folder keeps older mission variants that are no longer part of the default
working set but are still useful for tuning history and regression comparison.

Current contents:

- `runway_autoland_short_final_v7.waypoints`
  The short-final landing baseline that preceded the gentler v8 approach.
