## mini_talon_airspeed_lidar mission lane

This directory owns repeatable waypoint missions for the integrated Mini Talon airspeed + LiDAR stack.

Implemented file:
- `staircase_sensor_validation.waypoints`

The mission is aligned to the integrated `wind_staircase` world and the lane-local `plane_full.parm`.
